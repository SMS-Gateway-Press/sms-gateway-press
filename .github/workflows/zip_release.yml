name: Zip Release
on:
 release:
   types: [published]
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout code
       uses: actions/checkout@v2
     - name: Zip Release
       uses: TheDoctor0/zip-release@0.7.6
       with:
         archive_name: 'sms-gateway-press'
        #  exclude: '*.log, *.tmp'
